import {css} from "@emotion/css"

export const containerFluid = css`
    margin: 0 auto;
    max-width: 1200px;
    padding: 0 15px;
`

export const header = css`
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 0;
`
export const mobileHidden = css`
    display: block;
`
export const mobileVisible = css`
    display: none;
`